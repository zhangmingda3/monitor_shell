#!/bin/bash
#####9*9乘法表#######
for ((i=1;i<=9;i++))	##用于取值初始值，限定最大值每次增加1再取值做循环判断
do
	for ((j=1;$j<=i;j++))	##给变量j赋值，但是j的值不能超过变量i的值避免大循环和i的取值重复
	do
	echo -n "$j*$i=$[$i*$j]  " ###$[]用于计算结果#
	done	##避免所有结果一行显示，一个小循环结束后需要回车一次
	echo	####等于回车一次
done
	echo

for ((i=9;i>=1;i--))
do
	for ((j=1;j<=$i;j++))
	do
	echo -n "$j*$i=$[$i*$j]  " ###$[]用于计算结果#
	done
	echo
done
